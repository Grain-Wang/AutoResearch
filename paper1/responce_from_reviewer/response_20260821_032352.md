# Response to Review 2026-08-21 03:23:52 UTC

## 总体决定

接受“未达到强 CCF-C、不是 Paper Candidate”的判断。本轮重点修复未来即使得到正结果也无法解释的三类问题：

1. expert 训练内 prediction 污染 meta-router；
2. Claim-F 同时改变 semantic input 与 residualization algorithm；
3. 统计与特征协议允许 scene 相关性、候选相关 HV reference 和 intervention metadata 产生伪增益。

本轮没有生成真实数据、checkpoint、baseline 结果或科学结论。

## 已完成的代码改进

| 问题 | 修改 | 验证 | 状态 |
| --- | --- | --- | --- |
| router 使用 expert 训练内预测 | 新增 scene-group OOF stacking plan：router-train 第 k fold 只由排除 fold-k 的 experts 预测；dev/internal-test 只用 final experts | `cache_oof_experts.py` 与 `test_expert_cache_no_leakage.py` 检查唯一 prediction、training/prediction scene hash 零交集和 cache SHA | DONE-CODE FOR PLAN/AUDIT；真实训练/cache PENDING |
| feature 可读取 error/template metadata | 新增 feature denylist 与逐列 provenance/hash；直接或 one-hot/embedding 派生字段均硬失败 | `features.py`、`test_feature_schema_no_intervention_metadata.py` | DONE-CODE |
| image bootstrap 夸大显著性 | 新增 paired scene/drive cluster bootstrap；replicate 内重算 clean denominator、retention、CVaR、Pareto 与 HV | `bootstrap.py`、两-scene 手算测试 | DONE-CODE |
| HV reference 依赖候选集合 | 固定 `reference_cvar=1.05×dev always-D1 CVaR`，不读取候选 | dominated/duplicate/candidate-addition regression tests | DONE-CODE |
| image/scene ID 不能识别同帧别名 | source manifest 强制 `rgb_sha256, sequence_id, frame_index`；official test 检查 image/scene/sequence/RGB hash；sequence 不跨 split | split tests 加入 RGB alias 与 sequence leakage | DONE-CODE FOR GENERIC BUILDER；真实 adapters PENDING |

## 已完成的研究设计修正

| 审稿问题 | 本轮决定 | 文件 | 状态 |
| --- | --- | --- | --- |
| D0 用 image context、D1 用 text context，不是单变量对照 | 两者都读取相同 image-global/multi-scale features 和同构 text channel；D0 输入固定 zero/null text，D1 输入 caption；层名、shape、参数量、seed、FiLM 逐项一致 | `steps/005_frozen_experts.md` | SPECIFIED；PyTorch PENDING |
| 随机 expert diversity 伪装成语言价值 | 增加两个 image-only twin heads 与 shuffled-caption D1，全部使用相同 OOF stacking/router budget | Step 005 | SPECIFIED；结果 PENDING |
| Claim-F 的 C-B 同时改变输入和算法 | Claim-F 只使用同模型/同目标 `B-direct`、`C-direct`、`C-permuted`；Main-orth 不参与 Claim-F | `steps/006_semantic_incrementality_gate.md` | SPECIFIED；模型 PENDING |
| residual target 缺 inner OOF 与 final refit | 固定 outer-5/inner-4 scene-group state machine、minimum mean validation rank trial aggregation 和 full-train final refit | Step 006 | SPECIFIED；crossfit implementation PENDING |
| 缺少直接先行工作/基线 | 核验并加入 MRUF、DeferredSeg、Regression with Multi-Expert Deferral、Density-Ratio Post-Hoc L2D；四种 adaptation 全部成为 Claim-M 必需 baseline | `steps/002_related_work_audit.md`、`baseline_contract.yaml`、Step 007 | CONTRACT-DONE；baseline implementations PENDING |
| gate-only baselines 无法证明 routing 必要 | 加入 caption dropout、corruption augmentation、three-caption ensemble、consistency filtering；任一 robust expert 支配 Main 即停止 Claim-M | Step 007/008 | SPECIFIED |
| null caption 污染 local deletion risk | `target_deletion` 的三个 variants 全改为局部删除；完整 null caption 独立为 `null_diagnostic`，不进入 local worst-of-3/CVaR | `steps/003_intervention_dataset.md` | DONE-DESIGN；builder PENDING |
| “clean”标签含义过强 | 统一为 `predicate-clean`，只表示未检测到已启用 predicate；natural precision 禁止用生成规则循环验证 | Step 003/011 | DONE-DESIGN |
| 100 internal-test images 可能欠功效 | 300/100/100 降为 pilot；先做 annotation coverage 与 20-grid power simulation，power <0.80 必须增加独立 scenes | Step 003/008 | SPECIFIED；simulation/data PENDING |

## 直接近邻后的新颖性边界

- MRUF 已覆盖 leave-one-out error-increase modality teacher 与 uncertainty calibration；
- DeferredSeg 已覆盖 pixel-wise dense defer 与 spatial-coherence routing；
- Regression with Multi-Expert Deferral 已覆盖 bounded continuous loss 和 two-stage regression defer；
- Density-Ratio Post-Hoc L2D 已覆盖冻结 model/expert 的 scorer 与可调 threshold。

因此“区域 defer”“经验贡献教师”“连续 advantage defer”“冻结候选 post-hoc routing”全部撤回独立新颖性。Claim-M 只有同时显著击败这四类 adaptation、Risk-L2D-C 和 robust single-expert killers 才可能成立。

## 验证

```powershell
D:\Coding_APP\Anaconda\envs\auto_research\python.exe -m ruff check paper1
D:\Coding_APP\Anaconda\envs\auto_research\python.exe -m pytest paper1/tests -q --basetemp .local-deps/pytest-paper1-review3
```

- 当前全套测试：`22 passed`；
- Ruff：通过；
- 这些测试只验证协议实现，不是 NYUv2/KITTI 科学证据。

## 尚未完成

- NYUv2/KITTI 原始数据 adapters、真实 RGB hash manifest 和 `split_audit.json`；
- annotation coverage 与 power-analysis CSV；
- PyTorch 同构 D0/D1、OOF/final checkpoints、真实 expert cache；
- image twins/shuffled-caption control 结果；
- outer/inner crossfit model、AUROC 与 Claim-F control 结果；
- 四个 direct baselines、四个 robust expert baselines 与 artifact/grounding controls；
- official crop/valid-depth adapters、真实 cluster CI、latency 和主结果表。

当前 Claim-F/Claim-M 均为 `UNVERIFIED`，Paper Candidate 为“否”。

## 下一原子动作

先实现 NYUv2/KITTI source adapters 和 annotation-coverage audit；在真实局部 mask/depth 可用率以及 power 达标前，不启动 A800 大规模 expert 训练。KITTI 局部 oracle 失败时按预注册规则切换 Virtual KITTI 2 structured set，KITTI 只保留 image-level sensitivity/fallback。
