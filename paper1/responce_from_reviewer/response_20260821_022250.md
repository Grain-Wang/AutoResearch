# Response to Review 2026-08-21 02:22:50 UTC

## 总体决定

接受“当前未达到强 CCF-C、仍不是 Paper Candidate”的判断。本轮没有生成或暗示任何科学实验结果；完成的是三类修正：

1. 消除 official test 污染、H-defect 顺序矛盾和 L2D 输入不公平三项致命设计问题；
2. 将数据切分与指标定义推进为可运行、已测试的基础代码；
3. 把科学发现 Claim-F 与算法贡献 Claim-M 分开，并将 `Risk-L2D-C` 设为 Claim-M 的主要反对基线。

## 逐项回应

| 审稿问题 | 本轮修改 | 证据文件 | 状态 |
| --- | --- | --- | --- |
| official test/validation 参与 60/20/20 开发 | 改为只从 official training pool 取样；按 scene hash 划分 300/100/100；对 official test image/scene 做硬交集断言 | `steps/003_intervention_dataset.md`、`experiments/covol/build_image_manifest.py`、`tests/test_build_image_manifest.py` | DONE-CODE；真实 manifest PENDING |
| 004 在公平 D0 之前却声称 H-defect | 拆成 004-A `H-sensitivity` 和 004-B `H-fallback-defect`；正式顺序改为 `003 -> 005 -> 004-B -> 006` | `steps/004_defect_reproduction.md`、`paper1/README.md`、`steps/README.md` | DONE-DESIGN；实验 PENDING |
| TR2M checkpoint 无公平 image-only D0 | TR2M 只允许作 D1 clean/corrupt sensitivity 诊断；不再等待其训练代码；锁定仓库自有 shared-backbone dual-head | `steps/005_frozen_experts.md` | SPECIFIED；模型、训练脚本和 checkpoint PENDING |
| standard L2D 未获得同等语义特征/风险目标 | 分为 `L2D-B`、same-feature `L2D-C`、same-feature/same-objective `Risk-L2D-C`；learned gates 同容量、同训练步数、同 20-trial 预算 | `configs/covol/baseline_contract.yaml`、`steps/007_fair_gate_baselines.md` | CONTRACT-DONE；实现 PENDING |
| cross-fitting 的 K、fold、归一化、选参和 tie 未唯一化 | 固定 5-fold scene-group；fold-train 拟合全部预处理/nuisance，fold-validation 选参，fold-test 只产 OOF；每样本恰有一个 OOF；固定 20 trials 和 tie-band 算法 | `steps/006_semantic_incrementality_gate.md` | SPECIFIED；实现 PENDING |
| H-semantic 只看 AUROC，不保证策略价值 | 增加第二个硬门禁：C 相对 B 的 retention–CVaR Pareto hypervolume 差值 paired CI 下界必须 >0 | `steps/006_semantic_incrementality_gate.md`、`experiments/covol/metrics.py` | METRIC-DONE；probe PENDING |
| natural entity absence 可能来自不完整标注/检测漏检 | `entity_absence` 只允许声明 exhaustive 的类别且两套独立 detector/segmenter 均未检出；其他记录标 `unverified_mention`；每个 predicate×dataset 至少 200 正/200 负并要求 precision >=0.95 | `steps/003_intervention_dataset.md` | SPECIFIED；validator/data PENDING |
| natural error 的发生率与严重度混写 | 分别预注册 image-level incidence/Wilson CI 与相对公平 D0 的 regret severity；每个 dataset×captioner×predicate 至少 30 个 verified error 才做推断 | `steps/004_defect_reproduction.md`、`steps/008_canary_decision.md` | SPECIFIED；结果 PENDING |
| CVaR、worst-of-3、coverage、retention 口径不唯一 | 固定先在 image 内聚合 variants、再跨 image；定义正 regret 上尾 CVaR、eligible-region coverage、clean-gain CI stop rule 和 Pareto hypervolume | `steps/metrics_spec.md`、`experiments/covol/metrics.py`、`tests/test_metrics.py` | DONE-CODE |
| 16×16 hard-nearest 可能产生边界伪影 | 新增 global/8×8/16×16/32×32、hard-nearest/soft-bilinear 消融；单报 mask interior、5-pixel boundary、outside 与 depth-gradient error | `steps/009_component_ablation.md` | SPECIFIED；实验 PENDING |
| 缺少组件级先行工作 | 增补 DML/cross-fitting、Orthogonal Statistical Learning、CVaR optimization、Learn then Test 和 general risk control；明确均非独立创新 | `steps/002_related_work_audit.md` | DONE-DESIGN |
| 科学发现与算法贡献混写 | 拆为 Claim-F 和 Claim-M；Claim-M 只能由 Main 相对 `Risk-L2D-C` 的公平比较支持，不能由 H-semantic 代替 | `steps/001_primary_scope_lock.md`、`steps/008_canary_decision.md`、`steps/011_claim_language_revision.md` | DONE-DESIGN |

## 本轮可运行证据

环境：`conda auto_research`，Python 3.12.13。

执行命令：

```powershell
D:\Coding_APP\Anaconda\envs\auto_research\python.exe -m ruff check paper1
D:\Coding_APP\Anaconda\envs\auto_research\python.exe -m pytest paper1/tests -q --basetemp .local-deps\pytest-paper1
```

结果：

- Ruff：通过；
- Pytest：`7 passed`；
- split 单测覆盖确定性、scene 不跨 split、与 official test image/scene 零交集及泄漏硬失败；
- metrics 单测覆盖 worst-of-N/CVaR 聚合顺序、coverage 分母、clean-gain stop、Pareto hypervolume 和 tie band。

这些测试只验证代码口径，不是 NYUv2/KITTI 上的科学证据。

## 尚未完成与当前 blocker

- 尚未导入真实 NYUv2/KITTI official training/test manifests，因而没有 `image_manifest.jsonl` 或 `split_audit.json`。
- 尚未在仓库实现和训练 shared-backbone `D0/D1`，没有 checkpoint、SHA256 manifest 或 32-sample forward/backward smoke test。
- 当前 `auto_research` 环境没有 PyTorch；步骤 005 的 GPU 实现需要先在 A800 执行环境锁定 torch/CUDA 版本。
- 尚未实现 predicate validator、cross-fit probe、fair gate baselines 和 natural-error audit。
- 尚无 regret、AUROC、Pareto、置信区间、自然错误发生率或 latency 结果。

因此当前研究状态仍为 `Research Opportunity`，Paper Candidate 为“否”。

## 下一原子动作

优先实现 `experiments/covol/models/dual_candidate_depth.py`、`train_expert.py` 和 `tests/test_dual_candidates.py`，先在 32 个样本上验证结构同构、冻结 backbone、梯度隔离和参数量差异，再启动 seeds `17/29/43` 的正式训练。该动作不再依赖 TR2M 上游训练代码。
