# Response to Review 2026-08-21 01:33:39 UTC

## 总体决定

接受“当前未达到强 CCF-C、仍不是 Paper Candidate”的判断。本轮只完成研究范围、创新边界、算法假设和实验门禁修正，没有生成或暗示实验结果。

唯一主线已锁为 CoVoL-Depth；Q-GeoRoute 仅在 CoVoL Gate-0 被否定并更新范围锁后启动。

## 逐项回应

| 审稿问题 | 本轮修改 | 文件 | 状态 |
| --- | --- | --- | --- |
| 两条主线分散 | 锁定 CoVoL 唯一主线，Q-GeoRoute 降为备用 Phase 0 | `steps/001_primary_scope_lock.md`、Idea 2 | DONE-DESIGN |
| 近邻缺失、主张过宽 | 增补 8 项固定列矩阵；删除错误语言、冻结校准、细粒度文本、通用 gate 的新颖性主张 | `steps/002_related_work_audit.md`、Idea 1 | DONE-DESIGN |
| 只是标准 two-expert L2D | 将核心 Gate 改为 A/B/C nested probe；提出 cross-fitted nuisance-residual semantic increment，必须先证明 C-B 增量 | Idea 1 `5` | UNVERIFIED |
| oracle 随 router/residual 漂移 | 固定“独立训练 D0/D1 → 冻结/哈希 → 离线缓存 → router 只读”的两阶段协议 | Idea 1 `3` | BLOCKED-IMPLEMENTATION |
| D1 与公平性定义不清 | 固定共享冻结相对骨干、独立同结构 heads、参数量 ±10%、同数据/步数/种子、D1 不看 corruption | Idea 1 `3` | SPECIFIED |
| patch oracle 不唯一 | 固定 16×16 半开网格、有效像素阈值、mean AbsRel、边界规则和 nearest 上采样 | Idea 1 `4` | SPECIFIED |
| L_sparse 不能防止全关门 | 删除 L_sparse；预注册 `κ=0.80` clean-gain constraint，并用 primal-dual + CVaR@20% | Idea 1 `5` | UNVERIFIED |
| 因果/安全 overclaim | counterfactual→controlled intervention；value→empirical advantage；safe→selective/fallback-aware | `steps/011_claim_language_revision.md` | DONE-DESIGN |
| synthetic error/模板泄漏 | 自然错误与结构化错误分报；scene/template/captioner/error-family 隔离；held-out family quarantine | `steps/003_intervention_dataset.md` | SPECIFIED-NOT-BUILT |
| baseline 不公平 | 所有 gate 读取同一缓存/粒度/训练步数，参数量 ±10%；补齐 visual-only、difference、L2D 等 | Idea 1 `6` | PENDING |
| 统计单位和指标不足 | 图像/scene 为统计单位；3/5 seeds；10,000 paired bootstrap；retention/regret/CVaR/coverage/latency | Idea 1 `6` | PENDING |
| 无审计轨迹 | 新建步骤状态表，显式区分 design/result/blocker/STOP | `steps/README.md` | DONE |
| Q-GeoRoute 成本混淆 | 只保留 Lmax Phase 0；VGGT encoder/projector/LLM prefill/packing/end-to-end 分账 | Idea 2 `4` | PARKED |

## 新发现的实现阻塞

TR2M 上游锁定为 `a45925862bcd76c84ac38c6fc98da1e187f1146e`。截至审计日期，上游提供评测代码和权重，但 README 仍声明训练代码待发布。因此：

1. 发布权重可用于 H-defect；
2. 不能用 null caption 冒充公平 image-only D0；
3. H-semantic 前必须自行复现公平 calibration heads，或切换可独立训练 D0/D1 的公开基线；
4. 该状态记为 BLOCKED-IMPLEMENTATION，不记为负结果。

## 当前 claim status

- **科学事实：** 三篇新增近邻已覆盖语言深度鲁棒性、冻结视觉语言校准和长 caption 细粒度编码；TR2M 训练代码当前未公开。
- **研究假设：** 文本—区域语义在控制混杂后仍有 advantage 预测增量。
- **未验证方法主张：** cross-fitted semantic residual + clean-gain-constrained CVaR 优于标准 L2D。
- **当前最强反对意见：** 所有收益都可能由候选差异/视觉难度和普通 defer 解释。
- **Paper Candidate：否。**

## 下一步

严格执行 `003 → 004 → 005 → 006`。在 H-semantic 通过前不训练最终 router，不扩展 backbone/数据集，不启动备用方向。所有结果（包括失败）进入对应步骤文件和 results manifest。
